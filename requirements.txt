# 雀魂自动打牌 MOD - 依赖
# 安装: pip install -r requirements.txt

# 核心: MITM 代理拦截 WebSocket
mitmproxy>=10.0.0

# 鼠标/键盘模拟 (操作游戏)
pyautogui>=0.9.53

# 窗口查找 (定位游戏窗口)
pygetwindow>=0.0.9

# 全局热键监听 (F6/F7 快捷键)
pynput>=1.7.0

# 屏幕截图 + 图像识别 (按钮模板匹配)
opencv-python>=4.8.0
numpy>=1.24.0
