# 雀魂自动打牌 MOD - 依赖
# 安装: pip install -r requirements.txt

# 核心: MITM 代理拦截 WebSocket
mitmproxy>=10.0.0

# 鼠标/键盘模拟 (操作游戏)
pyautogui>=0.9.53

# 窗口查找 (定位游戏窗口)
pygetwindow>=0.0.9

# 全局热键监听 (F6/F7 快捷键)
pynput>=1.7.0

# 屏幕截图 + 图像识别 (按钮模板匹配)
opencv-python>=4.8.0
numpy>=1.24.0

# 网易 Airtest 图像识别引擎 (推荐, 更高精度)
# 安装: pip install airtest
airtest>=1.3.0

# DXGI 高速屏幕采集 (~3ms/帧, Windows only)
dxcam>=0.3.0

# PaddleOCR 分数/文字识别 (可选)
paddleocr>=3.0.0
paddlepaddle>=3.0.0
