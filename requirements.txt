# 雀魂自动打牌 MOD - 依赖
# 安装: pip install -r requirements.txt

# 核心: MITM 代理拦截 WebSocket
mitmproxy>=10.0.0

# 鼠标/键盘模拟 (操作游戏)
pyautogui>=0.9.53

# 窗口查找 (定位游戏窗口)
pygetwindow>=0.0.9
pyrect>=0.2.0

# 屏幕截图 (备选图像识别)
opencv-python>=4.8.0

# 辅助
pyserial>=3.5

# 前端界面 (可选)
# tkinter (内置)
